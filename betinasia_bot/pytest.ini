[pytest]
# Permite que testes `async def` rodem sem precisar decorar cada um com `@pytest.mark.asyncio`.
# Isso é importante porque vários testes de infraestrutura/scraper neste repositório são `async def`
# e foram escritos como smoke-tests.
asyncio_mode = auto

